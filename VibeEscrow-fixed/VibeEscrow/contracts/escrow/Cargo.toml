[package]
name    = "vibe-escrow"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
opnet = { git = "https://github.com/btc-vision/opnet-rust-sdk", branch = "main" }

[profile.release]
opt-level = "z"
lto       = true
strip     = true